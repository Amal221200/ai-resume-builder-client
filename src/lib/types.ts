export type TResume = {
    id: number,
    attributes: {
        title: string,
        resumeId: string,
        user_email: string,
        username: string,
        createdAt: string,
        updatedAt: string,
        publishedAt: string
    }
}